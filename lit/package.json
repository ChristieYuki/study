{
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "typescript": "^5.9.3"
  }
}
